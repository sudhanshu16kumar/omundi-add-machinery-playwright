This framework uses one shared image for every machinery record:

machine1.jpg

To use a different image later:
1. Put the new JPG/PNG in this folder.
2. Update only the `image` value inside the relevant content file:
   - data/rent-by-time.data.ts
   - data/rent-by-usage.data.ts

The automation flow itself does not need to change.
